services:
  - type: web
    name: pos-system
    env: python
    buildCommand: chmod +x build.sh && ./build.sh
    startCommand: gunicorn web_server:app
    pythonVersion: "3.11.4"
    envVars:
      - key: DATABASE_URL
        sync: false
      - key: PYTHON_VERSION
        value: "3.11.4"
